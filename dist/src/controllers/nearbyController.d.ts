import type { NextFunction } from "express";
import type { Request, Response } from "express";
export declare class NearbyController {
    private rescuerModel;
    constructor(rescuerModel: any);
    findNearbyRescuers: (req: Request, res: Response, next: NextFunction) => void;
}
//# sourceMappingURL=nearbyController.d.ts.map